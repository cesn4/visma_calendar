import React, { FunctionComponent } from 'react';

const App: FunctionComponent = () => {
    const className = 'app';
    return (
        <div className={className}>qq</div>
    );
};

export default App;
