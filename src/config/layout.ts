import { setConfiguration } from 'react-grid-system';

setConfiguration({
    containerWidths: [800, 1200, 1600, 1900],
    breakpoints: [376, 800, 1200, 1600],
    gutterWidth: 0,
    defaultScreenClass: 'lg',
});
